import React from 'react'
import { Screen } from 'components'

interface <%= props.pascalName %>Props{
  
}

export const <%= props.pascalName %>: React.FC<<%= props.pascalName %>Props> = (props) => {
    return (
      <Screen>
        <%= props.pascalName %>
      </Screen>
    );
}

